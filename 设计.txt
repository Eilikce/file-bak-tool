使用python 写一个 带界面的 文件备份工具：
1.动机：将一个目录中的所有文件夹中的所有文件，包括递归文件，扁平化的备份到一个目标目录，目标目录中保存所有文件，并且只有一层。
2.关键：将多级目录保存为扁平化，可能会遇到文件冲突的问题。请保证，同一个内容的文件，仅保存一份。判定同一个文件的标准是，文件名和文件内容，都完全一致。
3.支持增量备份，每次备份过程，都要检查文件的重复性，避免重复备份以提高效率。
4.如果遇到源文件，与目标文件夹中的文件重名，要检查文件内容是否也一致，如果内容不一致，则应该按一定规则，改名后存储到目标目录，避免文件丢失。
5.使用主流的GUI框架，一套代码，支持跨平台，支持交叉编译并打包，支持 Linux macOS windows
6.支持选择要备份的原路径，支持选择要将备份保存的目标路径。

请给出你的设计，中文回答


遇到需要重命名的情况时，重命名后的文件，也可能和既有文件重名，导致隐含的错误覆盖，请不要忘记，进一步检查重命名后文件名，是否依然与既有文件重名。。请一定要检查文件的内容，目标是，同一个文件内容，仅保存一份

没有外置数据库，我希望的是一个尽量轻量级的工具，你可以将数据和日志，简单的输出到备份的目标目录。这样是不是合理的？

重命名规则，应该是一个固定的可预判的规则。因为问可能会多次备份A目录到目标B目录，A目录的内容，可能每次会有所变化，我希望增量备份。这样的情况下，就可能会导致A目录中的旧文件多次备份到B目录，所以一个固定的可预判的重命名规则，可能更有利于你判断文件的重复性。是这样吗？或者你有什么更好的办法

我觉得，你的计划中，应该还要包含，完整功能的 单元测试 和 整体功能的集成测试。

你来完成所有的开发，和测试，并使用测试验证所有的功能，并为多平台打包。支持m1的macos，支持x64的windows